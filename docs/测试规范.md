# pty-agent 测试规范

> 本文档定义项目的测试策略、分类和运行方式。

---

## 测试分类

```
test/
├── unit/                     # 单元测试 — 隔离测试单一模块
│   ├── test_cli_optimization.py  # ConfigManager、Formatter 自然语言输出、debug 输出控制
│   ├── test_utf8_codec.py     # UTF-8 解码与尾部裁剪（codec）
│   ├── test_event_history.py # EventHistoryManager 事件历史管理
│   ├── test_factory.py       # create_pty 工厂函数
│   ├── test_gui_monitor.py   # GuiWindowMonitor 窗口检测
│   ├── test_handler.py       # RequestHandler 请求处理
│   ├── test_job.py           # ProcessJob (Job Object)
│   ├── test_main.py          # CLI 入口参数解析
│   ├── test_manager.py       # SessionManager 会话管理
│   ├── test_output_buffer.py # OutputBuffer 输出缓冲区
│   ├── test_process_info.py  # 进程信息工具函数
│   ├── test_process_monitor.py # ProcessMonitor + GuiDetector 进程监控
│   ├── test_pty_drain.py     # 各 PTY 后端 drain() 方法 + Session drain 集成
│   ├── test_pty_shell.py     # --shell 解释器选择 + --pty 冲突检测
│   ├── test_pty_subprocess.py# SubprocessPseudoTerminal 退出码
│   ├── test_session_events.py# 事件消费、崩溃自动返回、drain 集成
│   ├── test_shm_utils.py     # 共享内存工具函数（协议层 + 令牌）
│   ├── test_connect_robustness.py # Client 共享内存请求健壮性
│   ├── test_trigger.py       # TriggerMatcher 触发匹配 + safe_regex_search
│   ├── test_windows_error.py # Windows 错误码格式化
│   └── protocol/             # 协议层单元测试
│       ├── test_ansi.py      # ANSI 颜色/样式过滤（仅过滤 SGR，保留清屏/光标）
│       ├── test_encoding.py  # UTF-8 统一解码
│       └── test_message.py   # JSON 消息协议
├── integration/              # 集成测试 — 多模块协作
│   ├── test_session.py       # Session 退出码/状态/多会话
│   ├── test_single_instance.py # 单实例检查（共享内存）
│   └── test_debug_subprocess.py # 子进程调试辅助
```

---

## 运行方式

### pytest 测试（单元 + 集成）

```powershell
# 全部测试
python -m pytest test/ -v

# 仅单元测试
python -m pytest test/unit/ -v

# 仅集成测试
python -m pytest test/integration/ -v

# 指定文件
python -m pytest test/unit/test_job.py -v
```

### Protocol 协议层测试

可直接运行（独立于 pytest）：

```powershell
python test/unit/protocol/test_ansi.py
python test/unit/protocol/test_encoding.py
python test/unit/protocol/test_message.py
```

---

## 测试要求

### 单元测试

- **隔离**：不依赖外部进程、网络、文件系统
- **快速**：单测试 < 1s
- **平台标记**：Windows 专用测试用 `@pytest.mark.skipif` 跳过非 Win32
- **fixture**：使用 `@pytest.fixture` 管理 setUp/tearDown
- **断言**：使用 `assert`，非 `self.assert*`

### 集成测试

- **真实子进程**：创建实际的 Python 子进程验证退出码
- **超时保护**：所有等待循环带 deadline，超时后 `pytest.fail`
- **清理**：`autouse` fixture 确保测试结束后清理子进程
- **时长**：单测试 < 5s

---

## 添加新测试

1. 判断测试类型：
   - 单一类/函数、可 mock → `test/unit/`
   - 多模块协作、需子进程 → `test/integration/`
2. 按被测模块的文件名命名：`test_<模块名>.py`
3. pytest 测试函数以 `test_` 开头
4. 添加 Doxygen 风格的模块文档字符串

---

## 当前测试统计

> 注：以下为重构后（共享内存通信）实测数据，测试数量随代码演进变化。

| 分类 | 数量 | 运行方式 |
|------|------|---------|
| 单元测试 | 430 passed, 8 skipped | `pytest test/unit/` |
| 集成测试 | 26 passed, 1 skipped | `pytest test/integration/` |
| **pytest 总计** | **456 passed, 9 skipped** | `pytest test/` |
