{
  "id": "ai",
  "version": "2.0.0",
  "kind": "cli",
  "description": "CLI 侧 AI 二次分析：响应输出交给 aichat 分析并覆盖 outputStream",
  "commands": [
    "exec",
    "send",
    "read",
    "mouse"
  ],
  "hooks": {
    "check_request": {},
    "transform_response": {}
  },
  "permissions": {
    "required": [
      "filesystem.write",
      "filesystem.execute",
      "network.connect"
    ]
  }
}
