# =============================================================================
# setup.py - win_sandbox Python 包（兼容旧版 pip）
# =============================================================================
# 构建系统由 pyproject.toml 定义（hatchling），本文件仅供旧版 pip 回退使用。
# 实际构建配置见 pyproject.toml。
#
# 用法：
#   pip install -e .                  # 可编辑安装
#   pip wheel .                        # 构建 wheel
# =============================================================================

from setuptools import setup

if __name__ == "__main__":
    setup()