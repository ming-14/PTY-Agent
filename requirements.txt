# -*- coding: utf-8 -*-

# ===================== 运行时核心依赖 =====================

# TOML 配置解析（Python <3.11 回退，3.11+ 用标准库 tomllib）
tomli>=2.0; python_version < "3.11"

# 认证：Ed25519 公私钥签名、OpenSSH 密钥格式解析（CONNECT_MODE=tls 时需要）
cryptography>=41.0

# Web 前端 / 终端网关 (FastAPI 应用、WebSocket 服务)
fastapi>=0.111
uvicorn[standard]>=0.30
starlette>=0.37
websockets>=12.0

# Workflow 编排 (src/workflow/definition.py)：YAML 定义文件解析
pyyaml>=6.0

# MCP Server 封装 (mcp_server.py)：把 CLI 暴露为 MCP 工具
mcp>=1.0

# H.264 视频流编码 (src/screenshare/streamers/encoding/h264.py)
#   av    — PyAV，封装 ffmpeg 做视频编码/解码
#   numpy — 帧数据数值处理
numpy>=1.24
av>=11.0

# 进程信息/系统统计 (src/process、src/web/.../stats_provider.py)：
# 统一以 psutil 查询进程名/路径/命令行/内存/CPU/创建时间与进程树，勿手写 ctypes//proc
psutil>=5.9

# ===================== 可选依赖（缺失时自动降级） =====================
# 以下包在代码中均以 try/except ImportError 保护，未安装时相关功能降级，不影响主流程。
# 按需取消注释安装即可。

# wcwidth   — 终端字符显示宽度计算（CJK/零宽字符），供客户端渲染使用
wcwidth>=0.2

# scour     — SVG 压缩（--svg-compression-level 1/2），缺失时降级为 level 0
scour

# Pillow    — PNG/JPG/BMP 终端快照渲染 (src/client/renderer.py)，缺失时仅支持 .svg
Pillow>=10.0

# ===================== 测试依赖 =====================
pytest>=7.0
pytest-timeout>=2.0
